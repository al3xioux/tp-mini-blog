{% extends 'base.html.twig' %}

{% block title %}Nouvel article - Mini Blog
{% endblock %}

{% block body %}
	<style>
		.form-container {
			max-width: 800px;
			margin: 0 auto;
		}
		.form-header {
			margin-bottom: 2rem;
			padding-bottom: 1rem;
			border-bottom: 2px solid #1a1a1a;
		}
		.form-header h1 {
			font-size: 2rem;
			color: #1a1a1a;
			font-weight: 700;
		}
		.form-card {
			background: white;
			padding: 2rem;
			border: 1px solid #1a1a1a;
		}
		.back-link {
			display: inline-block;
			margin-top: 1.5rem;
			color: #666;
			text-decoration: none;
		}
		.back-link:hover {
			color: #1a1a1a;
			text-decoration: underline;
		}
	</style>

	<div class="form-container">
		<div class="form-header">
			<h1>Créer un nouvel article</h1>
		</div>

		<div class="form-card">
			{{ include('post/_form.html.twig', {button_label: 'Publier'}) }}
		</div>

		<a href="{{ path('app_post_index') }}" class="back-link">
			← Retour à la liste
		</a>
	</div>
{% endblock %}
